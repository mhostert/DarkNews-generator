[build-system]
requires = [
    "setuptools>=77",
    "setuptools-scm>=8",
    "wheel",
    "Cython>=3.0.11",
    "numpy>=2.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "DarkNews"
description = "A lightweight nu-A scattering generator for heavy neutral lepton production"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Asli Abdullahi" },
    { name = "Jaime Hoefken" },
    { name = "Matheus Hostert", email = "matheus-hostert@uiowa.edu" },
    { name = "Daniele Massaro" },
]
classifiers = [
    "Programming Language :: Cython",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "dill",
    "numpy",
    # numpy-2-compatible floors (these versions all work with numpy 1.x too):
    "scipy>=1.13.0",
    "pandas>=2.2.2",
    "matplotlib>=3.8.4",
    "vegas>=5.1.1",
    "Particle",
    "pyparsing",
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/mhostert/DarkNews-generator"
Repository = "https://github.com/mhostert/DarkNews-generator"

[project.optional-dependencies]
# Floors chosen so wheels exist for CPython 3.10-3.13 and the packages are
# numpy-2 compatible: pyarrow>=18 (first with cp313 wheels), pyhepmc>=2.16.1
# (first with cp312/cp313 wheels; older releases also trip on numpy>=2 via a
# removed VisibleDeprecationWarning import).
parquet = ["pyarrow>=18.0.0"]
pyhepmc = ["pyhepmc>=2.16.1"]
# Deep-inelastic scattering regime: pure-Python PDF evaluation (reads LHAPDF grids;
# no C++/LHAPDF dependency). PDF sets are installed with `python -m parton install`.
dis = ["parton>=0.2.2"]
testing = [
    "pytest>=6.0",
    "pytest-cov>=2.0",
    "tox>=3.24",
    "pyarrow>=18.0.0",
    "pyhepmc>=2.16.1",
]
dev = [
    "ruff>=0.6",
    "pre-commit",
]

[project.scripts]
dn_gen = "DarkNews.scripts:dn_gen"
dn_get_examples = "DarkNews.scripts:dn_get_examples"

[tool.setuptools]
zip-safe = false

[tool.setuptools_scm]
# The version is derived from git tags (vX.Y.Z). Between tags, builds get a
# dev version like 0.4.10.dev3+g1a2b3c4. `fallback_version` is used when git
# metadata is unavailable (e.g. building from a source zip with no .git).
fallback_version = "0.0.0"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = [
    "include/*/*.dat",
    "include/*/*.txt",
    "include/*/*.py",
    "include/assets/*",
    "*.pyx",
]
"DarkNews" = ["py.typed"]

[tool.pytest.ini_options]
addopts = "--cov=DarkNews"
testpaths = ["tests"]

[tool.cibuildwheel]
# Which interpreters to build for (see the CI matrix for the OS/arch dimension).
build = "cp310-* cp311-* cp312-* cp313-*"
skip = ["*-musllinux_i686", "*_i686"]
build-frontend = "build"
# Smoke-test each built wheel: install it and run the core suite. Optional-output
# tests (parquet/hepmc) skip gracefully when their deps are absent, so we only
# require pytest here; full-coverage testing lives in tests.yml (tox).
test-requires = ["pytest"]
test-command = "pytest {project}/tests -o addopts=''"

[tool.ruff]
line-length = 160
target-version = "py310"
src = ["src"]
# Bundled data-release scripts and notebooks are not part of the library and
# follow their own conventions (e.g. `from ROOT import *`); don't lint them.
extend-exclude = ["src/DarkNews/include/**", "examples/**"]

[tool.ruff.lint]
# Conservative starting set: pyflakes (F), pycodestyle errors/warnings (E/W),
# import sorting (I). Line length is handled by the formatter, so E501 is off.
select = ["E", "F", "W", "I"]
ignore = [
    "E501", # line length is handled by the formatter
    "E741", # ambiguous variable names (l, I) are common in physics notation
    "E402", # modules deliberately define their logger before importing submodules
    "E731", # terse `Power`/`Sqrt` lambda shims mirror the Mathematica CForm export
]

[tool.ruff.lint.per-file-ignores]
# __init__.py deliberately re-exports the public API (F401) and configures the
# loggers before importing submodules (E402). The import order is also
# load-bearing due to circular dependencies (MC <-> processes <-> integrands),
# so I001 (import sorting) must not touch it.
"__init__.py" = ["F401", "E402", "I001"]

[tool.ruff.format]
# Ruff's formatter replaces black; the line-length above is respected.
